import { SectionIndicatorDirective } from './section-indicator.directive';

describe('SectionIndicatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SectionIndicatorDirective();
    expect(directive).toBeTruthy();
  });
});
