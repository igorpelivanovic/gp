import { LoadingItemDirective } from './loading-item.directive';

describe('LoadingItemDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadingItemDirective();
    expect(directive).toBeTruthy();
  });
});
