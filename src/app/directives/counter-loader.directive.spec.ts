import { CounterLoaderDirective } from './counter-loader.directive';

describe('CounterLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new CounterLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
